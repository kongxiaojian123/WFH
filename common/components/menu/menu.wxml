<view class='menu-span'></view>
<view class="menu">
  <view 
    wx:for="{{menu}}"
    wx:key="{{index}}"
    data-index="{{index}}"
    class="{{index===menuIndex?'selected':''}}"
    bind:tap="selectMenu"
  >{{item}}</view>
</view>